name: PR Check

on:
  pull_request:
  push:
    branches: [ main ]

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Show files
        run: ls -la
      - name: Simple check
        run: echo "✅ PR check ran successfully"
      - name: Force fail (demo)
        run: exit 1
